---

  核心問題：怎麼定義排程「正確」

  排程正確性可以分成兩個層次：

  第一層：約束滿足 (Constraint Satisfaction) — 算法有沒有違反規則？
  第二層：失敗合理性 (Failure Justification) — 排不下的真的排不下嗎？

  這兩層合在一起，才能完整證明「算法沒有 bug」。

---

  第一層：Invariant 驗證（全部必須 100% 通過）

  這些是「不管輸入是什麼，都必須成立」的性質。任何一條不通過就代表算法有 bug：

  ┌───────────────┬───────────────────────────────────────────────────────────┬───────────────────────┐
  │   Invariant   │                           定義                            │     驗證的是什麼      │
  ├───────────────┼───────────────────────────────────────────────────────────┼───────────────────────┤
  │ 容量非負      │ 所有 capacity 的 curCapacity >= 0                         │ 算法沒有超額分配      │
  ├───────────────┼───────────────────────────────────────────────────────────┼───────────────────────┤
  │ 容量不超上限  │ 所有 capacity 的 curCapacity <= maxCapacity               │ Rollback 沒有多還容量 │
  ├───────────────┼───────────────────────────────────────────────────────────┼───────────────────────┤
  │ 數量守恆      │ SCHEDULED 訂單的 assignment 總量 = order.quantity         │ 沒有多排或少排        │
  ├───────────────┼───────────────────────────────────────────────────────────┼───────────────────────┤
  │ 時間窗合規    │ 所有 assignment 的 productionDate ∈ [startDate, deadline] │ 沒有排到窗口外        │
  ├───────────────┼───────────────────────────────────────────────────────────┼───────────────────────┤
  │ Rollback 完整 │ FAILED 訂單在結果中有 0 個 assignment                     │ 失敗回滾乾淨了        │
  ├───────────────┼───────────────────────────────────────────────────────────┼───────────────────────┤
  │ 不可變保護    │ isFixed / IN_PRODUCTION 訂單的狀態和 assignment 不被修改  │ 鎖定的訂單沒被動到    │
  └───────────────┴───────────────────────────────────────────────────────────┴───────────────────────┘

  Metric: Invariant Pass Rate = 通過的 invariant 數 / 總 invariant 數，目標永遠是 6/6 = 100%。

  你目前的 benchmark 只檢查了前兩項（容量非負 + 數量守恆），另外四項沒驗。

---

  第二層：FAILED 訂單的合理性證明（這是你問的核心）

  你說的完全對 — 「FAILED 多少筆」本身沒有意義，因為容量不夠就是排不下。真正要驗的是：

  ▎ 每一筆 FAILED 訂單，是否真的在它的時間窗內沒有足夠容量？

  具體做法 — 對每筆 FAILED 訂單做「事後獨立可行性檢查」：

  對每筆 FAILED 的訂單 O：
    1. 取得排程結束後的最終容量快照 (capacityMap)
    2. 計算 O 的時間窗 [startDate, deadline] 內，所有工廠的剩餘總容量
    3. 比較：剩餘總容量 vs O.quantity

```
→ 如果 剩餘容量 < quantity：  「Resource Contention」 — 容量被更高優先級訂單消耗了，合理
→ 如果 剩餘容量 >= quantity：  「Potential Miss」 — 算法有容量卻沒排進去，可能是 bug
```

  Metric: Failure Justification Rate = (總 FAILED 數 - Potential Miss 數) / 總 FAILED 數

- 100% = 每筆失敗都合理，算法邏輯正確
- < 100% = 有訂單明明排得下卻被標 FAILED，算法有問題

  注意：即使是 Greedy 演算法，因為處理順序的關係，某筆 FAILED 的訂單在排程結束後可能仍有剩餘容量放得下它 — 但那個容量是被
  「後處理的低優先序訂單」分走的。所以嚴格來說，你要用排程結束後的最終容量來判斷：如果最終都沒空間了，那這筆 FAILED
  是合理的。如果最終還有空間，需要進一步調查是排序問題還是 bug。

---

  第三層（選用）：效率指標

  這些不是「正確性」而是「品質」，但放在報告裡可以展現你們理解排程系統的深度：

  ┌──────────────┬────────────────────────────────────┬────────────────────┐
  │    Metric    │                公式                │        意義        │
  ├──────────────┼────────────────────────────────────┼────────────────────┤
  │ 排程成功率   │ SCHEDULED / (SCHEDULED + FAILED)   │ 整體排入比例       │
  ├──────────────┼────────────────────────────────────┼────────────────────┤
  │ 容量利用率   │ 已使用容量 / 總可用容量            │ 算法有沒有浪費空間 │
  ├──────────────┼────────────────────────────────────┼────────────────────┤
  │ 理論上界差距 │ 成功排入量 / min(總需求量, 總容量) │ 跟理想值差多遠     │
  └──────────────┴────────────────────────────────────┴────────────────────┘

---

  PPT 建議呈現法

  一頁圖就夠了，畫一個「正確性驗證金字塔」：

```
          ┌───────────────┐
          │  效率指標      │  ← 排程成功率、容量利用率
          │  (品質)       │     「排得好不好」
          ├───────────────┤
          │  失敗合理性    │  ← Failure Justification Rate
          │  (正確性)     │     「排不下的真的排不下嗎？」
          │  目標: 100%   │
          ├───────────────┤
          │  Invariant    │  ← 6 項約束全通過
          │  (必要條件)    │     「算法有沒有違反規則？」
          │  目標: 100%   │
          └───────────────┘
```

  然後用一個 benchmark 跑出來的實際數字表：

  測試規模：10,000 訂單 × 50 工廠 × 180 天
  ──────────────────────────────────────────
   Invariant 驗證        6/6 通過 ✅
   Failure Justification 100%    ✅
   排程成功率            92.3%
   容量利用率            87.1%
   執行時間              XXX ms

  這樣的呈現方式可以清楚回答評審的問題：「你怎麼知道排程是對的？」— 不是靠看 FAILED 數量，而是靠 invariant 全通過 + 每筆 
  FAILED 都能被數學解釋。

  Section 1: Invariant 6/6 ✅

  這證明了算法在 10,050 筆訂單的壓力下沒有違反任何約束：

- 容量沒有被扣成負數（沒有超額分配）
- 容量沒有被 rollback 多還（沒有回滾 bug）
- 每筆 SCHEDULED 訂單的 assignment 總量精確等於訂單數量（不多不少）
- 所有 assignment 都在合法時間窗內
- FAILED 訂單被乾淨回滾，沒有殘留 assignment
- isFixed 訂單完全沒被動到

  Section 2: Failure Justification 100%

  450 筆 FAILED 訂單每一筆都有合理原因：

- 92 筆 Window Conflict — 訂單交期太早，deadline 落在排程起始日之前，物理上不可能排入
- 358 筆 Capacity Exhausted — 排程結束後，這些訂單的時間窗內已無足夠剩餘容量，被更高優先序的訂單消耗了
- 0 筆 Greedy Trade-off — 沒有任何「明明有容量卻沒排進去」的情況

  100% Justification Rate = 算法沒有遺漏任何可行的排程機會。

  Section 3: Efficiency

- 95.5% Success Rate — 10,000 筆可排訂單中成功排入 9,550 筆
- 95.6% vs Theoretical Upper Bound — Greedy 的結果非常接近理論最佳值
- 40.9% Capacity Utilization — 因為系統總容量遠大於總需求（180 天 × 50 工廠），利用率自然不高，這是正常的





三種policy數據解讀

  

  比較表（直接截圖或重製到 PPT）

  ┌──────────────────────┬─────────────┬─────────────────┬─────────────────┐

  │         指標         │ GAP_FILLING │ GLOBAL_OPTIMIZE │ PRIORITY_RETAIN │

  ├──────────────────────┼─────────────┼─────────────────┼─────────────────┤

  │ 總排程成功           │ 10,764      │ 11,513          │ 10,764          │

  ├──────────────────────┼─────────────┼─────────────────┼─────────────────┤

  │ 新訂單成功 (/2000)   │ 1,164       │ 1,914           │ 1,164           │

  ├──────────────────────┼─────────────┼─────────────────┼─────────────────┤

  │ 既有訂單保留 (/9550) │ 9,550       │ 9,548           │ 9,550           │

  ├──────────────────────┼─────────────┼─────────────────┼─────────────────┤

  │ 既有訂單被擠掉       │ 0           │ 2               │ 0               │

  ├──────────────────────┼─────────────┼─────────────────┼─────────────────┤

  │ 穩定性               │ 100%        │ 100.0%          │ 100%            │

  ├──────────────────────┼─────────────┼─────────────────┼─────────────────┤

  │ 執行時間             │ 1,616 ms    │ 5,538 ms        │ 5,157 ms        │

  └──────────────────────┴─────────────┴─────────────────┴─────────────────┘

  三句話總結每個 policy

  ├──────────────────────┼─────────────┼─────────────────┼─────────────────┤

  │ 穩定性               │ 100%        │ 100.0%          │ 100%            │

  ├──────────────────────┼─────────────┼─────────────────┼─────────────────┤

  │ 執行時間             │ 1,616 ms    │ 5,538 ms        │ 5,157 ms        │

  └──────────────────────┴─────────────┴─────────────────┴─────────────────┘

  三句話總結每個 policy

  GAP_FILLING（預設 cron 排程用）

  - 既有排程完全不動，新訂單只填剩餘容量

  - 穩定性最高、速度最快（不需要重算既有排程）

  └──────────────────────┴─────────────┴─────────────────┴─────────────────┘

  三句話總結每個 policy

  GAP_FILLING（預設 cron 排程用）

  - 既有排程完全不動，新訂單只填剩餘容量

  - 穩定性最高、速度最快（不需要重算既有排程）

  GAP_FILLING（預設 cron 排程用）

  - 既有排程完全不動，新訂單只填剩餘容量

  - 穩定性最高、速度最快（不需要重算既有排程）

  GAP_FILLING（預設 cron 排程用）

  - 既有排程完全不動，新訂單只填剩餘容量

  - 穩定性最高、速度最快（不需要重算既有排程）

  - 既有排程完全不動，新訂單只填剩餘容量

  - 穩定性最高、速度最快（不需要重算既有排程）

  - 穩定性最高、速度最快（不需要重算既有排程）

  - 代價：新訂單成功率較低（1,164/2,000 = 58.2%），因為無法重新分配已佔用的容量

  - 代價：新訂單成功率較低（1,164/2,000 = 58.2%），因為無法重新分配已佔用的容量

  │         指標         │ GAP_FILLING │ GLOBAL_OPTIMIZE │ PRIORITY_RETAIN │

  ├──────────────────────┼─────────────┼─────────────────┼─────────────────┤

  │ 總排程成功           │ 10,764      │ 11,513          │ 10,764          │

  ├──────────────────────┼─────────────┼─────────────────┼─────────────────┤

  │ 新訂單成功 (/2000)   │ 1,164       │ 1,914           │ 1,164           │

  ├──────────────────────┼─────────────┼─────────────────┼─────────────────┤

  │ 既有訂單保留 (/9550) │ 9,550       │ 9,548           │ 9,550           │

  ├──────────────────────┼─────────────┼─────────────────┼─────────────────┤

  │ 既有訂單被擠掉       │ 0           │ 2               │ 0               │

  ├──────────────────────┼─────────────┼─────────────────┼─────────────────┤

  │ 穩定性               │ 100%        │ 100.0%          │ 100%            │

  ├──────────────────────┼─────────────┼─────────────────┼─────────────────┤

  │ 執行時間             │ 1,616 ms    │ 5,538 ms        │ 5,157 ms        │

  └──────────────────────┴─────────────┴─────────────────┴─────────────────┘

  三句話總結每個 policy

  GAP_FILLING（預設 cron 排程用）

  - 既有排程完全不動，新訂單只填剩餘容量

  - 穩定性最高、速度最快（不需要重算既有排程）

  - 代價：新訂單成功率較低（1,164/2,000 = 58.2%），因為無法重新分配已佔用的容量

  GLOBAL_OPTIMIZE（全局最佳化）

  - 釋放所有既有容量重新排程，排入最多訂單（11,513）

  - 新訂單成功率最高（1,914/2,000 = 95.7%）

  - 代價：2 筆原本已排程的訂單被擠掉（displaced），且執行時間最長

  PRIORITY_RETAIN（折衷方案）

  - 釋放容量重新排程，但優先保留已排程訂單的位置

  - 既有訂單 0 被擠掉，穩定性 = GAP_FILLING

  - 新訂單成功率與 GAP_FILLING 相同 — 在本實驗中，因為既有訂單優先排入，消耗了相同的容量

  PPT 上的定位

  用這張比較表放一頁就夠了，標題建議：

  ▎ 「排程策略比較：穩定性 vs 最佳化的設計取捨」

  然後附一句：「系統提供三種策略供管理者依據場景選擇 — 日常 cron 用 GAP_FILLING 保持穩定，需要重新最佳化時手動觸發

  GLOBAL_OPTIMIZE。」